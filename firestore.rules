rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global Safety Net: Open read & write access to ensure 100% reliability for all simulation data and managers
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
